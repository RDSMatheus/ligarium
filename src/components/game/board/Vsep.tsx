import React from "react";

function VSep() {
  return (
    <div
      className="self-stretch w-px shrink-0"
      style={{
        background:
          "linear-gradient(to bottom, transparent, rgba(200,144,10,0.22), transparent)",
      }}
    />
  );
}

export default VSep;
